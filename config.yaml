# =============================================================
# PILLTRACK SYSTEM CONFIGURATION (Hybrid Engine: DINO + SIFT)
# =============================================================

# --- 📦 Artifacts: ไฟล์สำคัญที่ต้อง Sync ขึ้น S3 ---
artifacts:
  model: "models/seg_best_process.pt"      # YOLOv8 weights สำหรับ Segmentation
  pack_vec: "database/pill_fingerprints.pkl" # [แก้แล้ว] ชื่อไฟล์ใหม่ที่มีทั้ง DINO และ SIFT ข้างใน
  drug_list: "database/drug_list.json"     # Metadata รายชื่อยา

# --- 📂 Paths: โครงสร้างโฟลเดอร์ ---
paths:
  db_images: "database_images"             # เก็บรูปต้นฉบับ (Producer only)
  models_folder: "models"                  
  db_pkl_folder: "database"                

# --- ⚙️ Settings: จูนค่า AI ---
settings:
  dino_size: 448                           # ขนาด Input DINO (ถ้า Pi ช้า ลดเหลือ 224 ได้)
  yolo_conf: 0.25                          # ความมั่นใจขั้นต่ำของ YOLO
  
  # [NEW] Hybrid Engine Weights (รวมกันต้องได้ 1.0)
  w_dino: 0.70                             # ให้ความสำคัญกับรูปลักษณ์รวม (DINO) 70%
  w_sift: 0.30                             # ให้ความสำคัญกับจุดรายละเอียด (SIFT) 30%
  sift_saturation: 20                      # จำนวนจุด SIFT match ที่จะถือว่า "เหมือนเป๊ะ" (Score=1.0)
  efficiency_target: 40                    # เป้าหมายจำนวนภาพต่อ Class

# --- 🖥️ UI & Display ---
display:
  width: 1280
  height: 720
  ui_zone_x: 850                           # จุดเริ่ม UI Zone ฝั่งขวา